# See 09-functions/README.md for more information
# about using functions in workflows, as well as
# commonly used functions and their features.

name: 09 - Using Functions

on:
  pull_request:
  workflow_dispatch:

jobs:
  echo1:
    runs-on: ubuntu-latest
    steps:
      - name: 1) Sleep for 20 seconds
        run: sleep 20
      # - name: Failing step
      #   run: exit 1
      - name: 2) I will be skipped
        if: ${{ success() }}
        run: echo "I will print if previous steps succeed."
      - name: 3) I will execute
        if: ${{ failure() }}
        run: echo "I will print if any previous step fails."
      - name: 4) I will execute
        if: ${{ !cancelled() }}
        run: echo "I will always print, except when the workflow is cancelled."
      - name: 5) I will execute when cancelled
        if: ${{ cancelled() }}
        run: echo "I will print if the workflow has been cancelled."